import { TabPlaceholder } from './TabPlaceholder';

export function InputsTab() {
  return (
    <TabPlaceholder
      title="Inputs"
      description="SKU CSV upload, Data Quality Dashboard, Ops Profile, Forward Drivers."
      phase="Phase 2 / 2.5"
    />
  );
}
