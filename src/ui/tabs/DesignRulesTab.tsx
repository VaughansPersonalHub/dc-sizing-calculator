import { TabPlaceholder } from './TabPlaceholder';

export function DesignRulesTab() {
  return (
    <TabPlaceholder
      title="Design Rules"
      description="Sigma, percentiles, soft-space, grid efficiency, per-zone aisle orientation, rack/MHE/automation selection."
      phase="Phase 1 / 3"
    />
  );
}
