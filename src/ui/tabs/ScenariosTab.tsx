import { TabPlaceholder } from './TabPlaceholder';

export function ScenariosTab() {
  return (
    <TabPlaceholder
      title="Scenarios & Tornado"
      description="Baseline plus variants, 17 curated tornado parameters, 4-worker pool run, feasibility filter."
      phase="Phase 6"
    />
  );
}
