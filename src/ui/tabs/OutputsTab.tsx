import { TabPlaceholder } from './TabPlaceholder';

export function OutputsTab() {
  return (
    <TabPlaceholder
      title="Outputs"
      description="Schedule of Areas, BOM, FTE, dock schedule, tornado chart, scenario compare, export panel (Excel, PDF, PPT, SVG, .scc)."
      phase="Phase 4 / 8"
    />
  );
}
